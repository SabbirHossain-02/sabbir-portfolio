import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the floating Next.js dev badge (the bottom-left "N") in development.
  devIndicators: false,
};

export default nextConfig;
